/// Virtual containers are containers that contain elements and provide virtual handles back to their
/// physical backings.
/// tl;dr they're containers without a generation counter

mod hash_map;
mod traits;